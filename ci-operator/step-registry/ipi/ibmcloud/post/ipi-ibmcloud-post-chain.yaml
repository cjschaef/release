chain:
  as: ipi-ibmcloud-post
  steps:
  - ref: gather-ibmcloud-resource
  - chain: ipi-deprovision
  documentation: |-
    The IPI cleanup step contains all steps that deprovision an OpenShift
    cluster on IBM Cloud, provisioned by the `ipi-ibmcloud-pre` chain.
